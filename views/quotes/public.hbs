{{#if quotes}}
    <div class="columns">
        {{#each quotes}}
            <div class="column card">
                {{> _quote}}
            </div>
        {{/each}}
    </div>
{{else}}
    <p>No quotes to show.</p>
{{/if}}
